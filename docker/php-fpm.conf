; Override the default PHP-FPM pool. It normally binds TCP :9000, but nginx
; now listens externally on :9000, so we move php-fpm to :9001 internally.
; Loaded after www.conf (alphabetical order in /usr/local/etc/php-fpm.d/).

[www]
listen = 127.0.0.1:9001
